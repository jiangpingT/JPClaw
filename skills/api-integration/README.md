# api-integration

Generic HTTP client for internal APIs.

## Input
JSON string.

## Output
JSON with status, headers, and body (truncated).

## Example
```json
{"method":"GET","url":"https://api.example.com/health","headers":{"X-Token":"..."}}
```
