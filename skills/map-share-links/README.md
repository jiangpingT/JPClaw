# map-share-links

Generate map share links for locations

## Input
- Plain text string passed to `run(input)`.

## Output
- Return a string result. Keep it concise.

## Example
```js
export async function run(input) {
  return `Processed: ${input}`;
}
```
